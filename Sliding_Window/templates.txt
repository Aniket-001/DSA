//------simple and concise templates----

int j=-1,cnt;
for(int i=0;i<length;i++){
    cnt+=nums[i];//inclusion
    //exclusion
    while(condn){
        j++
        cnt-=nums[j];
    }
    ans=Math.max(ans,i-j);
}
return ans;

###Important

For Exactly k ==  All Subarray with atmost(k) - All subarray with atmost(k-1)
