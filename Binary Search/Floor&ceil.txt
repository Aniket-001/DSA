int floor(int val,List<Integer> lst){
    if(val<lst.get(0)) return -1;
    int i=0,j=lst.size()-1,mid=0;
    int ans=-1;
    while(i<=j){
        mid = (i+j)>>1;
        if(lst.get(mid)>val) j=mid-1;
        else i=mid+1;
    }
    return j;
}
int ceil(int val,List<Integer> lst){
    if(val>lst.get(lst.size()-1)) return -1;
    int i=0,j=lst.size()-1,mid=0;
    while(i<=j){
        mid = (i+j)>>1;
        if(lst.get(mid)<val) i=mid+1;
        else j=mid-1;
    }
    return i;
}